#include "darts.h"
#include <stdio.h>
#include <stdint.h>
#include <math.h>

uint8_t score(coordinate_t landing_position) {
    float distance = 0;

    return 0;
}

int main() {
    printf("bru moment");
    //compiling!!!
    //had to modify the .h file - still don'r know what im doing too much but definitely enjoying it!
    return 0;
}