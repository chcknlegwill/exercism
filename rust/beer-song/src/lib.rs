pub fn verse(n: u32) -> String {
    //todo!("emit verse {n}");
    //I'm guessing n is the number of verses to sing 
    sing(n);
}

pub fn sing(start: u32, end: u32) -> String {
    //todo!("sing verses {start} to {end}, inclusive")
    //going to have to use a loop (look at documentation)
}
